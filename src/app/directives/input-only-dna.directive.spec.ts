import { InputOnlyDnaDirective } from './input-only-dna.directive';

describe('InputOnlyDnaDirective', () => {
  it('should create an instance', () => {
    const directive = new InputOnlyDnaDirective();
    expect(directive).toBeTruthy();
  });
});
